/*  const translate=require("./js/main")
 test('to check if the user has entered a command', () => {
    expect(translate("r")).toStrictEqual([1,0])
 });

 test('to check if the user has not entered a command', () => {
    expect(translate(" ")).toStrictEqual([0,0])
 }); */

